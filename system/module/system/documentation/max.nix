_:

{
  
  documentation = {
    enable = true;

    man = {
      enable = true;
      generateCaches = true;
    };
    
    doc = {
      enable = true;
    };

    dev = {
      enable = true;
    };

    nixos = {
      enable = true;
      # includeAllModules = true;
    };
  };

}
